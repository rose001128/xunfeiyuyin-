# iFlytek credentials
IFLYTEK_APPID=your_appid_here
IFLYTEK_APIKEY=your_apikey_here

# A strong token that your platform must send as `X-Plugin-Key`
PLUGIN_TOKEN=change_me_please

# Optional: bind & logging
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=info

# Optional: size guard (in bytes) for base64 string length
MAX_AUDIO_BYTES=8000000
